import 'package:flutter/material.dart';

class VisitRatingScreen extends StatelessWidget {
  const VisitRatingScreen({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(child: Text('Visit Rating Screen - TODO')),
      );
}