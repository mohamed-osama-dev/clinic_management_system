class AppConstants {
  const AppConstants._();

  static const String appName = 'ClinicEase (عيادتي)';
  static const String apiBaseUrl = 'https://api.example.com';
  static const Duration requestTimeout = Duration(seconds: 30);
}
